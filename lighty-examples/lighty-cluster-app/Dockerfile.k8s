FROM openjdk:11-jre-slim

COPY target/lighty-cluster-app-12.2.3-bin.zip /

RUN apt-get update && apt-get install unzip \
    && unzip /lighty-cluster-app-12.2.3-bin.zip \
    && rm /lighty-cluster-app-12.2.3-bin.zip

ENTRYPOINT ["/lighty-cluster-app-12.2.3/start-controller-node-k8s.sh"]
